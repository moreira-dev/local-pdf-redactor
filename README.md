# Local PDF Redactor

Lightweight tool to redact personal information PDFs.

This tool is completely local to maximise privacy. The redaction happens in your browser, so no data is sent to any server.

## To use it

Run `docker compose up`, then open [http://localhost:5173](http://localhost:5173).
