services:
  dev:
    image: node:24-slim
    working_dir: /app
    command: sh -c "corepack enable pnpm && pnpm install && pnpm dev --host"
    environment:
      COREPACK_ENABLE_DOWNLOAD_PROMPT: '0'
    ports:
      - '5173:5173'
    volumes:
      - .:/app
